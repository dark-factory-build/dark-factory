name = "dark-factory-control-plane"
main = "build/worker/shim.mjs"
compatibility_date = "2026-08-22"
workers_dev = false
preview_urls = false

[build]
command = "worker-build --release"

# Workers Logs. Without this the console_error diagnostics the Worker already
# emits -- the GraphQL error classes among them -- are discarded, so a typed
# failure GitHub named was unreadable exactly when it was needed (#371).
[observability]
enabled = true

[[durable_objects.bindings]]
name = "DARK_FACTORY_MAINTAINER_DELIVERIES"
class_name = "MaintainerDeliveryJournal"

[exports.MaintainerDeliveryJournal]
type = "durable-object"
storage = "sqlite"

[secrets]
required = [
    "DARK_FACTORY_MAINTAINER_WEBHOOK_SECRET",
    "DARK_FACTORY_MAINTAINER_WEBHOOK_SECRET_REVISION",
    "DARK_FACTORY_MAINTAINER_APP_ID",
    "DARK_FACTORY_MAINTAINER_PRIVATE_KEY_PKCS8",
    "DARK_FACTORY_MAINTAINER_PERMISSION_REVISION",
    "DARK_FACTORY_MAINTAINER_REPOSITORY",
    "DARK_FACTORY_MAINTAINER_REPOSITORY_OWNER_ID",
    "DARK_FACTORY_MAINTAINER_REPOSITORY_ID",
    "DARK_FACTORY_MAINTAINER_OPERATOR_EMAIL_SHA256",
    "DARK_FACTORY_CLOUDFLARE_ACCESS_TEAM_DOMAIN",
    "DARK_FACTORY_CLOUDFLARE_ACCESS_AUD",
]
